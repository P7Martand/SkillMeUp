# Test workspace

Used to demo SkillMeUp recommendations. Contains:

- `@anthropic-ai/sdk` and `@playwright/test` deps (should trigger `claude-api` and `webapp-testing` style skills if present in the catalog)
- A placeholder `report.pdf` file (should trigger a `pdf` skill suggestion if one exists)
- A `notebook.ipynb` placeholder (should trigger notebook-style skill suggestions)
